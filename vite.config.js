/**
 * vite.config.js
 * Vite build configuration — base path set for GitHub Pages deployment
 * under the /tracker-one/ sub-path.
 */
import { defineConfig } from 'vite';

export default defineConfig({
  base: '/tracker-one/',
});
